> **Provenance (transcription note by sis, not part of the verdict):** Verdict produced by the `oracle` subagent (session `ses_ff2e4acc8ffeUlen9PvewDJemp`, 8m36s) and transcribed verbatim below — Oracle is read-only, so sis performed the file write. ⚠️ Model routing warning from the harness: the oracle node fell back to `opencode-go/kimi-k3` (expected zai-coding-plan/glm-5.3). The verdict is empirically grounded (byte counts, grep of `wiki/**` frontmatter, full index read, log tail — all reproducible), but the operator should weigh the second opinion accordingly. Advisory until operator ratification; discussion-only lane.

---

# Oracle verdict: Main-vault sharing A+C + pi cost model (v0.2)

**Verdict:** SOUND-WITH-CONDITIONS.

A+C is architecturally consonant with the vault's own contract: QUERY is read-only by design (`~/Main-vault/AGENTS.md` line 158), archivist remains sole writer, and C carries the correct guard class (execution-receipt + verbatim-quote) for the known 2026-08-10 confabulation defect. pi's token arithmetic verified exactly against on-disk byte counts (index.md 25,621 B → 7,320 tok @ chars/3.5; AGENTS.md 37,665 B → 10,761; pi claimed 7.3K/10.8K). But both agents missed a measured fact that degrades the design's entry point: the vault's semantic layer is frozen — zero wiki pages with `date_updated` ≥ 2026-07 (grep across `wiki/**`, 254 pages), index last rebuilt 2026-06-29 (index.md line 3), vault hotcache `status: archived` 2026-06-02 — and all umbrella-project knowledge from the last ~7 weeks lives only in log.md session entries (08-05 dotfiles/ghostty/herdr, 08-07, 08-14, 08-15). QUERY as designed will systematically miss current umbrella state while returning June knowledge as if current. This is fixable with additive rules, not redesign — hence sound-with-conditions.

**Q1 — Design integrity:** A+C holds as designed. Failure modes missed by both (boundary-drift and governance-file editability stand as conceded):

1. **Discovery-layer freeze (measured).** sis's "zero staleness (live reads)" (sis-response §2) is true only for pi-vs-vault sync; vault-vs-reality is ~7 weeks stale at the discovery layer. Verified: zero `date_updated: 2026-07/2026-08` matches in `wiki/**`; index.md line 3 "Last updated: 2026-06-29"; hotcache.md frontmatter archived 2026-06-02. Meanwhile log.md carries the August umbrella work. pi flagged generic staleness but neither agent located where current knowledge actually lives nor priced log.md (15,005 B ≈ 4.3K full, ~1-2K tail) into the model. Consequence: index-first QUERY over current umbrella work misses; log.md is the only fresh artifact in pi's grant.
2. **Unverified-citation asymmetry.** C has receipt + verbatim-quote guards; A has none. pi's rule 4 ("wikilink citations trusted as audit trail") assumes faithful transcription — precisely the assumption falsified by the archivist defect (expected-vs-observed substitution, 5/8 vs corrected 8/8). A plausible citation attached to a misquoted page is *more* dangerous than an uncited claim because it passes casual audit. The read path needs the same guard class for load-bearing claims.
3. **Build-on pi's divergence con:** with C deliberately near-zero-volume, store.jsonl-vs-vault divergence is the *steady state*, not an edge case. "Preserve both, never average" handles noticed contradictions; silent drift has no reconciliation pass.
4. **C latency:** operator-arbitrated, no SLA → unsuitable for time-sensitive writes. Fine at near-zero volume; should be stated so pi never parks urgent knowledge on the lane.

**Q2 — Three-tier framing:** Sound, with two clarifications.

- The taxonomy matches the vault's own: `[[operational-memory-for-coding-agents]]` (four-layer semantic/operational/working/procedural, index.md line 140) and the letta/mem0 entity pages (index lines 106-107) document the same tiering. The collective-memory synthesis independently validates the mapping: hotcache = individual working memory, wiki = collective durable memory (`wiki/synthesis/collective-memory-contribution.md`, lines 46-64). T3-as-vault is a correct mapping, not a stretch.
- **Skills-independence preserved.** A is filesystem reads governed by a clause in pi's own AGENTS.md; pi re-implements QUERY discipline rather than invoking any opencode skill. No skill-runtime dependency is created; provider coupling is unchanged (reads flow through pi's existing provider).
- The real couplings are (a) *schema* — pi must interpret `status`/`date_updated` frontmatter (one-time selective read, cheap) and (b) *availability* — T3 must remain best-effort. pi operates today without the vault; that invariant holds as long as pointer facts are hints, never load-bearing dependencies. State this explicitly in the clause.
- Structural note: "The pheromone trail is only as good as the deposits" (collective-memory-contribution.md, Conclusions #4). pi is a near-non-depositing reader by design — the "cross-agent single source of truth" pro is asymmetric (sis deposits, pi consumes). Not a defect; do not let it become an expectation that pi's T2 learnings will reach the vault.

**Q3 — Cost-control rules:** Mostly sufficient; arithmetic verified; the pointer approach is sound *because of* vault doctrine, with two required bounds.

- **Arithmetic (measured):** index.md 25,621 B / 3.5 = 7,320 ✓; AGENTS.md 37,665 B / 3.5 = 10,761 ✓; wiki-page band 5-30KB → 1.4-8.6K internally consistent. Caveat: chars/3.5 is prose-calibrated; index.md is link-dense list markup — real tokenization plausibly 10-20% higher; treat 7.3K as a floor (~8-9K realistic). Missing line item: log.md ~4.3K full / 1-2K tail — cheap and necessary (Q1 finding 1).
- **The 5-10× reuse claim holds only on hits:** 12-22K vs 50-150K = 2.3-12.5×. The model omits P(miss): on a miss the vault cost is pure tax atop research still required. Verified coverage asymmetry: no dotfiles/ghostty/herdr entries in index.md (full read); opencode well-covered (3 entries). Hit rate will be low for umbrella-config questions, high for agent-architecture/memory/skills questions. The fix is a rule, not a new model.
- **Pointer facts: NOT worse than the staleness they mitigate** — because the vault's archival doctrine makes existence pointers self-healing (AGENTS.md lines 331-344: never hard-delete referenced pages; superseded pages carry `superseded_by` redirects). Worst case = pointer resolves to an archived page that redirects to current. Failure asymmetry favors pointers: stale content asserted as fact vs. a stale pointer costing one extra query. Two required bounds: (a) pointers carry existence+topic only, never content claims (content decays fast, existence barely decays); (b) cap the pointer set (~40) with periodic prune — store.jsonl already injects 123 facts/turn, and unbounded pointer growth re-creates the injection bloat rule 2 exists to prevent.
- **Rule gaps (additive):** abandon-early (index scan + ≤1 page with no coverage → stop; cap vault tax ≈ 8-10K/question); log-tail reads for currency-sensitive questions; verbatim-quote for load-bearing claims; rule 4 sharpening — currency = per-page `date_updated`, NOT LINT flags (LINT is manual-trigger only, §4 line 205; no scheduled lint exists, so `needs_review` flags cannot be assumed present).
- Cache note on rule 3: repeated index scans within one session may be discounted if pi's provider bills cached prefix tokens lower — the vault's own KV-cache page documents 0.1× cached pricing with measured 1.47× per-file savings and 10× only at system-prompt prefix sharing (`skill-layout-kv-cache.md` lines 125-147). Provider-dependent; treat as possible bonus, not baseline.

**Q4 — Scaling:** QUERY scales past umbrella expansion only with hardened read discipline; full-scan collapses first, and B is never the right fallback.

- Measured basis: 254 pages → 25.6KB index ≈ 100 B/entry, linear. Full-scan cost: 500 pages ≈ 50KB ≈ 14-16K tok; 1,000 pages ≈ 100KB ≈ 28K+. Collapse threshold: when scan cost exceeds the value of the 1-3 pages it locates — index ≈ 40-60KB, i.e. **~400-600 pages**, full-scan discipline breaks (scan alone >50% of a 22K query budget).
- Umbrella reality: dotfiles/ghostty/herdr/pi currently have ~zero wiki coverage; if the operator ingests the umbrella, page count plausibly doubles-triples — hitting the full-scan ceiling within the design's lifetime.
- Fallback path, in order (all vault-owned, all live — B stays rejected for sis's stated reason: a second export artifact duplicates the index and adds staleness):
  1. grep-first + section-scoped index reads (stable category sections — read the relevant ~1/6th, or grep matched lines for ~hundreds of tokens). Extends the comfortable ceiling to ~2-5K pages. Mandatory once index > ~40KB — pi's rule 1 "where possible" is too soft.
  2. Curated per-project entry pages inside the vault (e.g. `umbrella-dotfiles.md` as sub-index). Any curation small enough to export as a digest is better expressed as a vault-owned page: no second artifact, no sync, no staleness.
  3. Beyond that, sharded per-category index files, still vault-owned.
- B-as-fallback is a category error at any size: it moves curation out of the vault into a copy. Trigger for step 2: index > ~60KB or sustained miss-driven full scans, whichever first.

**Q5 — Vault AGENTS.md contradictions:** None found; one consistency confirmation, one latent collision already covered, one doctrine the design must not lean on.

- **§8c (lines 962-1002):** governs the bd-vs-vault boundary for the vault's own second-brain pilot. Rule 1 (operational in Beads, durable in vault, table lines 971-978) is *consistent* with pi's T2/T3 split — the design maps onto it cleanly. Rule 4's "multi-agent: read `DOLT.md` first" (line 978) scopes to Dolt remote sync for Beads, not to adding a vault reader; single-writer containment is untouched, so §8c is not triggered. Rule 2 sync-back (line 976; §8b lines 936-944) explains the observed drift: August sync-backs landed in log.md (verified) without wiki/index distillation — internal vault drift, not caused by this design, but the reason the log-tail condition exists.
- **LINT (§4, lines 203-217):** report-only, human-triggered ("Request: 'Lint the wiki'", line 205). Does not contradict the design — but the design must not treat it as an automated safety net; pi's cons list ("LINT flags these") overstates it. Currency assessment must use per-page `date_updated` (mandated frontmatter, lines 51-74).
- **Latent collision, already covered:** Rule 11 (line 633) instructs the vault's maintainer LLM to "Read hotcache at session start" — directly opposed to pi's cost rule 1. Vault AGENTS.md is written as imperatives to "the LLM"; the data-not-instructions clause is therefore load-bearing, and the pi-side clause should state explicitly: vault workflows (INGEST/CONTRIBUTE/hotcache/LINT) are not pi's workflows.
- **Consonance note:** QUERY is explicitly read-only (line 158) — A formalizes an existing sanctioned workflow rather than inventing an access pattern. The proposed one-line operator edit to the QUERY section is the right formalization, correctly assigned operator-only.

**Conditions & recommendations:** (effort to adopt: Quick (<1h) — all fold into the already-approved pi-side clause text, the one-line vault note, and the one bd constraint; no new infrastructure)

1. Add abandon-early to pi's clause: index scan + ≤1 page, no coverage → stop; vault tax capped ≈ 8-10K/question. Misses must be priced, not just hits.
2. Currency-sensitive queries include a log.md tail read (~1-2K tokens) — verified: wiki/index frozen at June; August umbrella knowledge exists only in log entries.
3. Symmetric verbatim-quote guard on A: load-bearing vault claims quote the page verbatim beside the `[[wikilink]]` — same standard as C's receipt guard.
4. Pointer facts restricted to existence+topic, never content; capped (~40) with periodic prune; subject to the existing T1 no-auto-promotion rule.
5. Currency judged from `date_updated` frontmatter only; LINT flags never assumed present.
6. Hotcache grant names exactly `~/Main-vault/hotcache.md` (archived, low value, low risk); `~/.sisyphus/hotcache.md` excluded — sis session-handoff, outside T3 semantic scope.
7. Read discipline hardens with size: grep-first/section-scoped index reads mandatory once index.md > ~40KB; at ~60KB or sustained full-scan misses, operator adds curated per-project entry pages in-vault (B remains rejected).
8. Bound divergence: pi runs one reconciliation query per month (or per ~30 sessions) re-checking its vault-pointer facts against live pages; C declared non-urgent-only (no SLA on the arbitrated lane).
9. T3 stated as best-effort in pi's clause: pi must remain fully correct with the vault absent; no vault read may become a hard dependency.

This verdict is advisory until operator ratification; the lane remains discussion-only; no writes were performed — every claim above was verified by read-only inspection of the cited files this session.
